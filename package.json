{
  "name": "cytest-automation",
  "version": "1.0.0",
  "type": "module",
  "description": "CompassionCare Cypress Test Automation Suite",
  "main": "cypress.config.js",
  "scripts": {
    "test": "cypress run",
    "test:ui": "cypress open",
    "test:cli": "cypress run",
    "test:cli:chrome": "cypress run --browser chrome",
    "test:cli:spec": "cypress run --spec 'cypress/e2e/pages/login.cy.js'",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "cypress": "^13.17.0",
    "cypress-mochawesome-reporter": "^5.0.0"
  },
  "dependencies": {
    "@testing-library/cypress": "^10.1.3"
  }
}
